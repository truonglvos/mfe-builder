import { GetTypeResizePipe } from './get-type-resize.pipe';

describe('GetTypeResizePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTypeResizePipe();
    expect(pipe).toBeTruthy();
  });
});
