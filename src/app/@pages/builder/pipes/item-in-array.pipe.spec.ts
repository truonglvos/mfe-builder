import { ItemInArrayPipe } from './item-in-array.pipe';

describe('ItemInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
